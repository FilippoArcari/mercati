# Project Dependencies - Mercati

# Core Dependencies (from pyproject.toml)
fredapi>=0.5.2
google-api-python-client>=2.193.0
google-auth-httplib2>=0.3.1
google-auth-oauthlib>=1.3.1
hydra-core>=1.3.2
matplotlib>=3.10.0
pandas>=2.3.0
pandas-datareader>=0.10.0
python-dotenv>=1.2.2
torch>=2.10.0
pytorch-lightning>=2.0.0
stable-baselines3>=2.0.0
yfinance>=0.2.66

# Transitive Dependencies (explicitly declared for clarity)
numpy>=1.26.0
scipy>=1.14.0
scikit-learn>=1.5.0
seaborn>=0.13.0

# ── Acceleratore hardware (installa UNO dei seguenti) ─────────────────────────
#
# CUDA (GPU NVIDIA T4 / P100 / etc.):
#   pip install torch --index-url https://download.pytorch.org/whl/cu118
#   oppure lascia la dipendenza torch>=2.10.0 sopra (Kaggle la gestisce in auto)
#
# TPU v5e-8 (Kaggle TPU runtime):
#   torch_xla è pre-installato automaticamente — non serve aggiungerlo.
#   Su GCP TPU VM manuale:
#     pip install torch_xla[tpu] -f https://storage.googleapis.com/libtpu-releases/index.html
#
# In locale (sviluppo su CPU):
#   nessuna dipendenza aggiuntiva — il codice rileva automaticamente CPU come fallback.

# Python Version Requirement
# Python >= 3.13
