# Project Dependencies - Mercati
# Generated from pyproject.toml
# Python >= 3.13

alpaca-py>=0.43.2
fredapi>=0.5.2
google-api-python-client>=2.193.0
google-auth-httplib2>=0.3.1
google-auth-oauthlib>=1.3.1
gymnasium>=1.2.3
hydra-core>=1.3.2
lightning>=2.6.1
matplotlib>=3.10.0
nolds>=0.6.3
optuna>=4.8.0
optuna-integration[pytorch-lightning]>=4.8.0
pandas>=2.3.0
pandas-datareader>=0.10.0
powerlaw>=2.0.0
pyarrow>=24.0.0
pyinform>=0.2.0
python-dotenv>=1.2.2
pytorch-lightning>=2.6.1
scikit-learn>=1.5.0
scipy>=1.17.1
seaborn>=0.13.0
stable-baselines3[extra]>=2.8.0
tensorboard>=2.20.0
torch>=2.10.0
yfinance>=0.2.66

# ── Hardware Accelerator (install ONE of the following) ──────────────────────
#
# CUDA (GPU NVIDIA T4 / P100 / etc.):
#   pip install torch --index-url https://download.pytorch.org/whl/cu118
#   or keep torch>=2.10.0 above (Kaggle handles it automatically)
#
# TPU v5e-8 (Kaggle TPU runtime):
#   torch_xla is pre-installed automatically — no need to add it.
#   On manual GCP TPU VM:
#     pip install torch_xla[tpu] -f https://storage.googleapis.com/libtpu-releases/index.html
#
# Local (CPU development):
#   no additional dependencies needed — the code automatically detects CPU as fallback.
